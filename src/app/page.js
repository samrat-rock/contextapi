import React from 'react'
import UserContextProvider from './context/UserContextProvider'

function page() {
  return (
    <UserContextProvider>
      
    </UserContextProvider>
  )
}

export default page
